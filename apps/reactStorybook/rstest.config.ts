import { defineConfig } from '@rstest/core';

export default defineConfig({
  // Migrated from jest.config.ts (babel-jest with react presets)
  // Rstest uses SWC by default - no transform config needed
});
